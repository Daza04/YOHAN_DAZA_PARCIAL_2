# Configuración de Base de Datos MySQL
DB_HOST=db
DB_NAME=app_db
DB_USER=appuser
DB_PASSWORD=secure_password_123

# Notas:
# - Copiar este archivo a .env antes de ejecutar docker-compose
# - Cambiar DB_PASSWORD por una contraseña segura en producción
# - DB_HOST debe ser 'db' (nombre del servicio en docker-compose)
